[pytest]
pythonpath = .
markers =
    unit: fast, deterministic unit tests that do not require network or external services.
    integration: broader tests that exercise cross-component or real dependency behavior.
    network: tests that require outbound network access (disabled by default unless explicitly enabled).
